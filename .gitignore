# mpv runtime cache (shader/icc caches, remote-control pid/lock)
portable_config/cache/

# Python bytecode
__pycache__/
*.pyc

# mpv log
portable_config/LOG.txt

# webmods console error dumps
portable_config/errors-*.txt

# per-title track picks, written at runtime
portable_config/scripts/smart-track-selector/track_preferences.json
